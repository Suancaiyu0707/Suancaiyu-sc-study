server:
  port: 3377

spring:
  application:
    name: sc-nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # 服务注册中心
      config:
        server-addr: localhost:8848 # 服务注册中心
        file-extension: yaml # 指定yaml格式的配置
        group: TEST_GROUP
        namespace: 60bf0383-1b80-4782-bfba-693309a51346
#        group: DEV_GROUP #默认是DEFAULT 组
